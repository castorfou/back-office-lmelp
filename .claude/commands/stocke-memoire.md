1. Stocke ta memoire dans un fichier markdown sous @docs/claude/memory, en prefixant ce fichier par yymmdd-hhMM

2. **IMPORTANT - Liens vers le code** :
   - N'utilise JAMAIS de liens markdown vers des fichiers de code source (ex: `[file.vue](path/to/file.vue)`)
   - Utilise uniquement des backticks avec le chemin complet (ex: `` `frontend/src/views/CalibreLibrary.vue` ``)
   - Pour les numéros de ligne, utilise le format `` `file.vue:203` `` ou `` `file.vue:202-210` ``
   - Les liens markdown sont réservés aux URLs externes (GitHub, documentation web)

3. Indique le chemin quand c'est fait
